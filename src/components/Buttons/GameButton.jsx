import React from "react";

import Button from "react-bootstrap/Button";

const GameButton = ({ to, text }) => {
  return (
    
      <Button variant="Info">{text}</Button>
    
  );
};

export default GameButton;