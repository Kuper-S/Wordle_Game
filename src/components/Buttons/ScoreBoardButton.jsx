import React from "react";

import Button from "react-bootstrap/Button";

const ScoreBoardButton = ({ to, text }) => {
  return (
    
      <Button variant="light">{text}</Button>
    
  );
};

export default ScoreBoardButton;